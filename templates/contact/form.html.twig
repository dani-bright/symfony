{% extends 'parent.html.twig' %}

{% block content %}
    <div class="col">
        {% include 'inc/flashMessage.html.twig' %}
        {#
        form : variable envoyée par le controleur
            form_label : balise label
            form_widget: champ du formulaire
            form_start : balise form début
        #}
        {#
            attr : attribut html à unchamp
        #}
        {{ form_start(form, { attr : { novalidate: 'novalidate'}}) }}
        {{ form_label(form.firstname, 'prénom') }}
        {{ form_widget(form.firstname, { attr : { placeholder: 'Saisissez votre prénom'}}) }}
        {{ form_label(form.lastname, 'nom') }}
        {{ form_widget(form.lastname, { attr : { placeholder: 'Saisissez votre nom'}}) }}
        {{ form_label(form.email, 'Email') }}
        {{ form_widget(form.email, { attr : { placeholder: 'Saisissez votre email'}}) }}
        {{ form_label(form.message, 'Message') }}
        {{ form_widget(form.message) }}
        <input type="submit" value="Valider" class="btn btn-primary">
        {{ form_end(form) }}

    </div>
{% endblock %}