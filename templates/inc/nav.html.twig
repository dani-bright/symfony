<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ url('homepage.index') }}">{{ site_name }}</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ url('homepage.index') }}">Home <span class="sr-only">(current)</span></a>
            </li>
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown"
                   aria-haspopup="true" aria-expanded="false">
                    Oeuvres
                </a>
                <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                    <a class="dropdown-item" href="{{ url('artwork.index') }}">Tout</a>
                    <a class="dropdown-item" href="{{ url('artwork.category', {'category': 1})}}">Peinture</a>
                    <a class="dropdown-item" href="{{ url('artwork.category', {'category': 3})}}">Dessin</a>
                    <a class="dropdown-item" href="{{ url('artwork.category', {'category': 2})}}">sculpture</a>
                </div>
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item active">
                    <a class="nav-link" href="{{ url('admin.index') }}">administration<span
                                class="sr-only">(current)</span></a>
                </li>
            {% endif %}
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item active">
                    <a class="nav-link alert-danger" href="{{ url('security.logout') }}">deconnexion<span
                                class="sr-only">(current)</span></a>
                </li>
            {% else %}
                <li class="nav-item active">
                    <a class="nav-link alert-success" href="{{ url('security.login') }}">connexion<span class="sr-only">(current)</span></a>
                </li>
            {% endif %}

            <li class="nav-item">
                <a class="nav-link" href="{{ url('expo.index') }}">Exposition</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ url('contact.form') }}">contact</a>
            </li>

        </ul>
    </div>
</nav>